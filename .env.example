# Database connection string
# For Railway: Use the DATABASE_URL provided by the Railway PostgreSQL plugin
DATABASE_URI=postgres://postgres:postgres@localhost:5432/<your-db-name>

# Used to encrypt JWT tokens
# Generate a secure secret: openssl rand -hex 32
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
# For Railway: Set this to your Railway app URL (e.g., https://your-app.up.railway.app)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
